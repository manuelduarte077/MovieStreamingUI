import 'package:flutter/material.dart';

class DetailsSoccerScreen extends StatelessWidget {
  const DetailsSoccerScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('DetailsMovieScreen'),
      ),
    );
  }
}
